from app.models.features.scai_pipeline import build_features, LOOKBACK_BASE, VIS_FEATURES

print("scai_pipeline OK")
print(f"LOOKBACK_BASE: {len(LOOKBACK_BASE)} features")
print(f"VIS_FEATURES:  {len(VIS_FEATURES)} features")
